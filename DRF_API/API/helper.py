def modify_input_for_multiple_files(answer_sheet, image, image_order):
    dict = {'answer_sheet': answer_sheet,
            'image': image,
            'image_order': image_order
            }
    return dict